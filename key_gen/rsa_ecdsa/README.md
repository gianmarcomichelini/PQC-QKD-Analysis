# RSA and ECDSA Key Generation Script

Generates RSA and ECDSA keypairs using OpenSSL.

## Algorithms

- **RSA:** 2048, 3072, 4096 bits  
- **ECDSA:** P-256 (`prime256v1`), P-384 (`secp384r1`), P-521 (`secp521r1`)

## Usage

```sh
./generate_keys.sh
